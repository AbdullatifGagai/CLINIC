@using ZCLINIC;

@model IEnumerable<ZCLINIC.Areas.Master.Models.Student>

@{
    bool addview = ZCLINIC.UserRights.URM_AddNew_IsAuthenticated(Context.Session.GetRoleId(), "87");
    bool editview = ZCLINIC.UserRights.URM_Update_IsAuthenticated(Context.Session.GetRoleId(), "87");
    bool delete = ZCLINIC.UserRights.URM_Delete_IsAuthenticated(Context.Session.GetRoleId(), "87");
    bool Print = ZCLINIC.UserRights.URM_Print_IsAuthenticated(Context.Session.GetRoleId(), "87");
}




<div class="page-header page-header-default">
    <div class="breadcrumb-line">
        <h6 class="text-semibold">Admission Form</h6>
    </div>
</div>

<div class="content">
    <div class="panel panel-flat">
        <div class="panel-body">
            <div class="row">
                <div class="col-md-3 buttons-top">

                    @if (addview)
                    {
                        <a class="btn btn-success btn-sm" data-modalhorizontal="true" data-modalsize="modal-full" data-url="/Master/Student/AdmissionSAI" data-heading="Admission Form" data-script="#"><i class="icon-add mr-5"></i> Add</a>
                    }



                </div>

                <div class="col-sm-6">
                    <div class="form-group">
                        @*<select class="bootstrap-select data-type" data-style="btn-primary" data-width="20%">
                                <option value="0">All</option>
                                <option value="1">Active</option>
                                <option value="2">Inactive </option>
                            </select>*@

                        @*<select url="/Master/MasterReports/City" class="bootstrap-select" id="downloadBtn" data-style="btn-primary" data-width="30%">
                                <option value="0">[SELECT Type]</option>
                                <option value="1">PDF</option>
                                <option value="2">EXCEL</option>
                            </select>*@
                    </div>

                </div>

                <div class="col-sm-3">
                    <div class="input-group ">
                        <input type="text" class="form-control" id="FilterInput">
                        <span class="input-group-addon bg-blue-800"><i class="icon-search4"></i></span>
                    </div>
                </div>
            </div>

            <table class="table table-xxs table-md ServerSidePagination table-bordered table-striped table-hover bg-lightgray dataTables_scrollBody table-responsive">
                <thead>
                    <tr class="bg-blue-800">
                        <th>GRNo</th>
                        <th>Student Name</th>
                        <th>level</th>
                        <th>Plan</th>
                        <th>Seat No</th>




                        @*  <th>Status</th>*@
                        <th>Actions</th>

                    </tr>
                </thead>
                <tbody>
                    @*@foreach (ZCLINIC.Areas.Master.Models.Student s in Model)
            {
                <tr data-rowid="@s.GRId">

                    <td>@s.Grno</td>
                    <td>@s.SName</td>
                    <td>@s.LevelName</td>
                    <td>@s.PlanName</td>
                    <td>@s.SeatNo</td>



                    <td>
                        <ul class="icons-list">
                            @if (editview)
                            {
                                <li class="text-primary-600">

                                    <a data-modalhorizontal="true" data-modalsize="modal-full" data-heading="Edit Admission Form" data-script="#" data-url="/Master/Student/AdmissionSAI?Id=@s.GRId"><i class="icon-pencil7"></i></a>
                                </li>
                            }
                            @if (delete)
                            {
                                <li class="text-danger-600"><a onclick="DeleteAlert({ 'Id':@s.GRId}, '/Master/Student/DeleteStudent', 'Student Admission')"><i class="icon-trash"></i></a></li>
                            }





                        </ul>
                    </td>
                </tr>
            }*@
                </tbody>
            </table>
        </div>
    </div>
</div>

<script>

    @*var addNew = @Html.Raw(Json.Serialize(addview));*@
    var editview = @Html.Raw(Json.Serialize(editview));
    var deleteview = @Html.Raw(Json.Serialize(delete));
    var Printview = @Html.Raw(Json.Serialize(Print));

</script>